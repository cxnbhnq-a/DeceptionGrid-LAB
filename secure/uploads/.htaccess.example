# Security: Prevent direct access to uploaded files except images
# This prevents execution of uploaded PHP files or access to sensitive files

<Files "*">
    Order Deny,Allow
    Deny from all
</Files>

<FilesMatch "\.(jpg|jpeg|png|gif)$">
    Order Deny,Allow
    Allow from all
</FilesMatch>